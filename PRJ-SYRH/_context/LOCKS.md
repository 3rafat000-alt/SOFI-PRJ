# LOCKS — PRJ-SYRH  (path claims; check before editing a shared path — git-discipline §5)
> `sofi claim PRJ-SYRH <path>` to claim · `sofi release PRJ-SYRH <path>` to drop.
> Already-claimed path → use a worktree or serialize via the lead.
